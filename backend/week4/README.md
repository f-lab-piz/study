# Week 4

OpenAI API, `requests`, OpenAI Python SDK, LangChain, LangGraph 예제를 모아둔 주차 실습입니다.

## 실행

```bash
uv sync
uv run python main.py
```

`.env` 파일에 `OPENAI_API_KEY`가 필요합니다.

## 파일

- `main.py`: 예제 실행 파일
- `pyproject.toml`: `uv` 프로젝트 설정
- `uv.lock`: 잠금 파일
